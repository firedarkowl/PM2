public class Eintrag  implements Comparable<Eintrag> {
    @Override
    public int compareTo(Eintrag o) {
        return 0;
        //hier gehört eine sinnvolle Implementierung mit -1, 0 und 1
        //todo Video 6 ab 1:37:00
    }
}
